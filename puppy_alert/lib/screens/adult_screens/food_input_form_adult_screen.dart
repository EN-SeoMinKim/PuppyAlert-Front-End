import 'package:flutter/cupertino.dart';

class FoodInputFormAdultScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          child: Text("form"),
        ),
      ],
    );
  }
}
